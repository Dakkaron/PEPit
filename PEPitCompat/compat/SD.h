/* PEPitCompat — SD Shim (empty stub, SD_MMC is the actual interface used) */
#ifndef SD_H
#define SD_H

// Empty stub — all SD card operations go through SD_MMC namespace
// This header exists for compatibility with code that includes <SD.h>

#endif // SD_H
